N = 5
weights = [1,2,3,4,5]


queue = {}
for cycle in range(N):
    while queue:
        test = dict()
        cases = []
        test = cases.pop(0)
        queue.pop()
        queue.
        (f'{value}',f'{occupation}') = 'cases'
        for j in range(N):
            v = '{}.occupation'
            if not j in v:
                val
                
                if sum(left_set) >= sum(right_set) + weight:
                    right_set.append()
val, used_set, 